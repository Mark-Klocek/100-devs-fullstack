//Create an array of movies with at least three movies.
let movieArray = ['star wars','pitch black','the godfather','weenies']
//Using the array from above, store the first movie in a variable
let movieVariable = movieArray[0]
//Get the length of the original array and store it in a new variable
let movieArrayLength = movieArray.length
//Get the last element in that array and store it in a new variable. What if your array was really large and you didn't know the last index? Would your solution still work?
let lastMovieInArray = movieArray[movieArray.length - 1]
console.log(lastMovieInArray)